# Spark `.spark/` store inventory

This inventory is the reviewable contract for local Spark runtime state under a project
root. It documents the current owners and guarantees; it does not require every store to
have the same durability level.

## Scope classes

- **Project canonical/protected** stores are project-level Spark state. They are not safe
  housekeeping targets except through a typed owner API or a future explicit cleanup
  command with user intent.
- **Session cache** stores live under the project `.spark/` directory but are scoped to a
  Pi session, owner session, or display cache. They may be stale after sessions finish and
  are eligible for housekeeping when the owning workflow is terminal or the cache is
  broken/stale.
- **Legacy/import-only** stores are read for migration or compatibility only. New writes
  must go to the current owner API.
- **Ad hoc scratch** paths are human/agent files under `.spark/` without typed schemas.
  Prefer typed artifacts for durable workflow evidence.

## Global transaction rules

- `.spark/` is local runtime state. It should be ignored by Git; durable shared knowledge
  must be exported or committed explicitly outside `.spark/`.
- Each typed store has one owner package/API. Callers should go through that API instead
  of writing JSON files directly. Where the current implementation has no dedicated store
  API, that gap is called out below rather than hidden.
- Store transaction boundaries are per store file or per artifact metadata file. Spark does
  **not** provide database-style cross-store transactions.
- Multi-store flows must tolerate partial success. For example, task execution may update
  `projects.json` and create artifacts separately; `/spark` initialization creates graph,
  TODO, review gate, and artifacts in separate commits.
- When a store writes content-addressed blobs plus metadata, metadata is the commit point.
  Orphan blobs are acceptable and may be reported by housekeeping; they are not evidence
  until referenced by committed metadata.
- Session caches should be treated as recoverable hints. Losing or deleting a cache file
  must not delete canonical project/task/artifact history.

## Inventory

| Path                                         | Owner package/API                                                                                                                          | Scope                                                                                                      | Read/write API                                                                                                                                                                                                                     | Transaction boundary                                                                                                                                                                                                                                                                                                                           | Locking and atomicity                                                                                                                                                                                                                                                                           | Housekeeping / legacy policy                                                                                                                                                                                                                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.spark/projects.json`                       | `spark-tasks` / `TaskGraphStore`                                                                                                           | Project canonical/protected                                                                                | `defaultTaskGraphStore(cwd)`, `load()`, `save()`, `update()`, `withLock()`                                                                                                                                                         | One full `TaskGraph` snapshot containing projects, tasks, dependencies, runs, claims, readiness, and TODO summaries                                                                                                                                                                                                                            | `update()` performs locked read-modify-write. `save()` outside an existing lock acquires `.spark/projects.json.lock`, checks loaded graph staleness when possible, writes a temp file, then renames.                                                                                            | Never delete as cache. Use `spark-tasks` APIs for changes.                                                                                                                                                                                                                                         |
| `.spark/projects.json.lock/`                 | `spark-tasks` / `TaskGraphStore` internals                                                                                                 | Project lock metadata                                                                                      | Internal lock directory with `owner.json` heartbeat                                                                                                                                                                                | Lock acquisition for `projects.json` RMW                                                                                                                                                                                                                                                                                                       | Created with exclusive `mkdir`; stale locks are removed after heartbeat expiry; release removes only when owner id matches.                                                                                                                                                                     | Housekeeping may remove only stale lock directories; do not treat as data.                                                                                                                                                                                                                         |
| `.spark/todos/<session>.json`                | `spark-tasks` data model via `TaskTodoStore`; scoped by `spark` facade                                                                     | Session cache for task TODOs                                                                               | `defaultTaskTodoStore(cwd, scope)`, `TaskTodoStore.load/save/hydrate()`, tool facade `spark_update_task_todos`                                                                                                                     | One scoped TODO snapshot. It intentionally stays outside `projects.json` to avoid high-churn checklist writes in graph snapshots.                                                                                                                                                                                                              | Current implementation writes the file directly with `writeFile`; no per-file lock or atomic rename yet. Graph updates and TODO updates are non-transactional relative to each other.                                                                                                           | Eligible for housekeeping when broken, stale, empty, all TODOs are terminal, and referenced tasks are terminal/missing. `defaultTaskTodoStore` requires a scope; `.spark/todos.json` is no longer a runtime store entrypoint.                                                                      |
| `.spark/sessions/<owner>.json`               | `spark` extension facade                                                                                                                   | Session-owner cache                                                                                        | Internal helpers `loadCurrentProjectState()`, `saveCurrentProjectRef()`, `saveSparkExecutionMode()`, `clearCurrentProjectRef()`; user-facing `spark_use_project` / command mode flows                                              | One owner-scoped sessions and optional execution-mode pointer                                                                                                                                                                                                                                                                                  | Current implementation writes directly with `writeFile`; no lock or atomic rename. Data is advisory and can be rebuilt from user/tool selection.                                                                                                                                                | Safe to delete when broken, stale, points to a missing/done project, or belongs to an inactive owner.                                                                                                                                                                                              |
| `.spark/session-todos/<session>.json`        | `spark` extension facade                                                                                                                   | Session cache for independent session TODOs                                                                | Internal `loadIndependentTodos()` / `saveIndependentTodos()`; user-facing `spark_update_todos`                                                                                                                                     | One scoped independent TODO list                                                                                                                                                                                                                                                                                                               | Current implementation writes directly with `writeFile`; no lock or atomic rename.                                                                                                                                                                                                              | Safe to delete when broken, empty, or stale with all TODOs terminal.                                                                                                                                                                                                                               |
| `.spark/todo-display-numbers/<session>.json` | `spark` extension facade UI/tool cache                                                                                                     | Session display cache                                                                                      | Internal `loadTodoDisplayNumberState()`, `assignTodoDisplayNumber()`, `saveTodoDisplayNumberState()`                                                                                                                               | One scoped mapping from stable TODO keys to display numbers                                                                                                                                                                                                                                                                                    | Current implementation writes directly with `writeFile`; no lock or atomic rename. Losing it only renumbers display labels.                                                                                                                                                                     | Safe to delete when broken or stale.                                                                                                                                                                                                                                                               |
| `.spark/workflow-runs.json`                  | `spark-orchestrator` / `SparkDagRunStore`                                                                                                  | Project canonical/protected Spark workflow-run state                                                       | `defaultSparkDagRunStore(cwd)`, `status()`, `startRun()`, `recordSchedule()`, `recordProgress()`, `finishRun()`, `reconcile()`, `acknowledgeFailures()`, `clearInactiveRuns()`                                                     | One workflow-run snapshot with manager status and workflow invocation records                                                                                                                                                                                                                                                                  | Writes use `.spark/workflow-runs.json.lock/` for serialized read-modify-write updates, then temp-file + rename for the snapshot. Terminal runs ignore late schedule/progress mutations; known problem runs can be marked with `acknowledgedAt` / `acknowledgedBySession` instead of deleted.    | Protected. Prefer `SparkDagRunStore.acknowledgeFailures()` for known failures; cleanup must use `SparkDagRunStore.clearInactiveRuns()` or an explicit future cleanup API, not file deletion.                                                                                                       |
| `.spark/artifacts/<artifact-id>.json`        | Physical owner `spark-artifacts` / `ArtifactStore`; logical producers include `spark`, `spark-runtime`, `spark-ask`, and `spark-learnings` | Project canonical/protected artifact metadata                                                              | `defaultArtifactStore(cwd)`, `put()`, `update()`, `get()`, `getBody()`, `tryGet()`, `list()`, `linksTo()`, `diff()`, `compactMetadata()`; `spark_state action:"compact-role-run-artifacts"` for historical role-run blob retention | One artifact metadata file per `ArtifactRef`; metadata is the commit point for artifact existence; historical transcript replacement records `transcriptRetention` with original blob/hash/size, replacement summary, tail, optional export path, and deletion time                                                                            | Current implementation writes blob first, then metadata with direct `writeFile`; no lock or atomic rename. Updating an existing ref is last-writer-wins. Historical role-run retention writes replacement metadata with temp-file + rename before deleting the referenced full transcript blob. | Protected. Do not delete metadata as cache. Metadata compaction is explicit and dry-run by default; it rewrites verified metadata only and never deletes blobs. Historical role-run full transcript blob deletion is also dry-run by default and requires replacement summary/tail metadata first. |
| `.spark/artifacts/blobs/<hash>.<ext>`        | `spark-artifacts` / `ArtifactStore`                                                                                                        | Project content-addressed blob store                                                                       | Written by `ArtifactStore.put()`; read by `getBody()` / hydrated `get()`                                                                                                                                                           | One immutable body blob addressed by content hash                                                                                                                                                                                                                                                                                              | Written before metadata; no separate lock. Blob is evidence only when referenced by committed metadata.                                                                                                                                                                                         | Orphan blobs are acceptable. Housekeeping may report them; deletion requires explicit cleanup policy.                                                                                                                                                                                              |
| `.spark/artifacts/*` learning records        | Logical owner `spark-learnings`; physical owner `spark-artifacts`                                                                          | Project canonical/protected artifacts with logical learning scope (`global`, `project`, `project`, `task`) | `defaultLearningStore(cwd)`, learning record/search/list/read/lifecycle/export/import helpers                                                                                                                                      | Same artifact metadata/blob boundary as `spark-artifacts`                                                                                                                                                                                                                                                                                      | Same physical guarantees as artifact storage. Learning status changes are artifact updates, not a separate HOME-level store.                                                                                                                                                                    | Share learnings only through explicit export/import or committed Markdown, not by copying `.spark/`.                                                                                                                                                                                               |
| `.spark/artifacts/*` ask-answer records      | Logical owner `spark-ask`; physical owner `spark-artifacts`                                                                                | Project canonical/protected ask artifacts                                                                  | `spark_ask`, `spark_ask_replay`, `createAskArtifactBody()`                                                                                                                                                                         | Same artifact metadata/blob boundary as `spark-artifacts`; body stores `{ request, result, summary }`                                                                                                                                                                                                                                          | Same physical guarantees as artifact storage. Decision/approval/no-selection semantics live in the artifact body and caller policy.                                                                                                                                                             | Protected workflow evidence. Do not infer approval from missing or cancelled artifacts.                                                                                                                                                                                                            |
| `.spark/artifacts/*` role-run records        | Logical owner `spark-runtime`; physical owner `spark-artifacts`                                                                            | Project canonical/protected execution evidence                                                             | `runSparkTask()` persists `kind: "role-run"` artifacts with task/run provenance; `spark_state action:"compact-role-run-artifacts"` can retain compact summaries for legacy oversized transcripts                                   | Same artifact metadata/blob boundary as `spark-artifacts`; task status updates remain separate `projects.json` writes. New role-run bodies are compact summaries with bounded stdout/stderr/json-event tails; legacy oversized full transcript blobs can be replaced by compact summary + serialized transcript tail and optional export path. | Same physical guarantees as artifact storage. Artifact creation and task graph update are non-transactional relative to each other. Historical retention apply copies optional export first, writes replacement metadata, then deletes the full transcript blob.                                | Protected execution evidence. Full transcript blob deletion is never part of generic cache cleanup; use dry-run preview and explicit apply only after candidates are reviewed.                                                                                                                     |
| `.spark/review-gate.json`                    | `spark` initialization/review flows                                                                                                        | Project canonical/protected review gate                                                                    | Current init flow creates and writes the review gate JSON file directly                                                                                                                                                            | One review gate JSON file                                                                                                                                                                                                                                                                                                                      | Current implementation writes directly with `writeFile`; no lock, atomic rename, or dedicated store abstraction yet.                                                                                                                                                                            | Protected. Ownership should be tightened before adding more writers. Review details should prefer typed review artifacts.                                                                                                                                                                          |
| `.spark/roadmap.json`                        | `spark` roadmap planning flow                                                                                                              | Project canonical/protected roadmap planning layer                                                         | Internal helpers in `packages/spark/src/flows/roadmap-flow.ts`; `/plan` reads active/matching items and `spark_plan_tasks` attaches produced project/task refs                                                                     | One project roadmap snapshot with active roadmap/item pointers and roadmap items                                                                                                                                                                                                                                                               | Writes use temp-file + rename; there is no lock yet, so concurrent roadmap updates are last-writer-wins. Graph/task updates and roadmap ref attachment are non-transactional relative to each other.                                                                                            | Protected prototype state. Do not delete as cache; evolve toward a dedicated store API before adding more writers.                                                                                                                                                                                 |
| `.spark/notes/`                              | No typed package owner; human/agent documentation area                                                                                     | Project protected ad hoc notes                                                                             | File tools / manual Markdown files only                                                                                                                                                                                            | Per file; not a typed transactional store                                                                                                                                                                                                                                                                                                      | No Spark lock or atomicity guarantee.                                                                                                                                                                                                                                                           | Protected from automatic cache cleanup. Use for local notes/design scratch; use typed artifacts or committed docs for durable/shareable records.                                                                                                                                                   |
| `.spark/role-reports/`                       | No typed package owner; role-run scratch convention                                                                                        | Ad hoc scratch                                                                                             | File tools / role-run reports only                                                                                                                                                                                                 | Per file; not a typed transactional store                                                                                                                                                                                                                                                                                                      | No Spark lock or atomicity guarantee.                                                                                                                                                                                                                                                           | Not canonical workflow state. Prefer typed `role-run` artifacts for evidence that should survive cleanup.                                                                                                                                                                                          |

## Owner boundaries by package

- `spark-tasks` owns the `TaskGraph` schema and the task TODO store schema. It must not
  execute roles or write Spark artifacts.
- `spark-artifacts` owns physical artifact metadata and blobs. Producers own the meaning
  of their artifact `kind` and body, but not the filesystem layout.
- `spark-learnings`, `spark-ask`, `spark-runtime`, and review flows own logical artifact
  bodies while delegating persistence to `spark-artifacts`.
- `spark-orchestrator` owns workflow-run invocation state in `workflow-runs.json`; it does not
  own task graph snapshots.
- `spark` owns Pi extension/session caches (`sessions`, `session-todos`,
  `todo-display-numbers`), the prototype roadmap planning layer (`roadmap.json`), and the
  housekeeping summary that classifies cache vs protected state.
- `pi-roles` does not own Spark workflow state and does not read `.spark/` role
  compatibility paths at runtime.

## Housekeeping strategy

Housekeeping must classify before deleting:

1. **Protected project state:** `projects.json`, `artifacts/`, `workflow-runs.json`,
   `review-gate.json`, `roadmap.json`, and `notes/` are report-only unless a typed owner cleanup API says
   otherwise.
2. **Session caches:** `sessions/`, `todos/<session>.json`, `session-todos/`, and
   `todo-display-numbers/` may be reported as safe-to-delete when stale, broken, empty, or
   tied only to terminal/missing work according to the cache-specific rules above.
3. **Legacy state:** `todos.json` should be explicitly retired; no runtime API
   writes or loads it as the default task TODO store.
4. **Scratch:** `role-reports/` and untyped files are not canonical evidence. Cleanup
   should be conservative and prefer reporting before deletion.
