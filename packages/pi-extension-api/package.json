{
  "name": "pi-extension-api",
  "version": "0.1.0",
  "description": "Shared TypeScript contract surface (types only) for Pi-style extensions consumed by both pi-coding-agent and the spark-cli native host.",
  "license": "MIT",
  "author": "zrr1999",
  "files": [
    "src/**/*.ts",
    "README.md"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc -p ../../tsconfig.json --noEmit"
  }
}
