# spark-tasks

Thread/task DAG orchestration. Tasks belong to threads, dependencies are task-to-task, executable tasks bind to registered agents, each task carries dynamic TODO state, and each thread can track a current interaction task.
