# spark-text

Spark-owned text layout helpers (`truncateToWidth`, `visibleWidth`, `wrapTextWithAnsi`) for packages that should not depend on `@zendev-lab/spark-tui`.

Direct `@earendil-works/pi-tui` usage stays behind this package and `@zendev-lab/spark-tui`.
