# spark-goal

Spark-owned goal execution primitives for `/workflow:goal` strategy wiring.

This package vendors and renames selected MIT-licensed core from `pi-codex-goal`. It intentionally does not expose upstream slash-command registration, global Pi session storage, or extension entry points.
