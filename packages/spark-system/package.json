{
  "name": "@zendev-lab/spark-system",
  "version": "0.1.0",
  "private": true,
  "description": "Spark filesystem path, permission, command, and local runtime helpers.",
  "files": [
    "src",
    "!src/**/*.test.ts",
    "package.json",
    "README.md"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./strings": "./src/strings.ts",
    "./daemon-local-rpc": "./src/daemon-local-rpc.ts",
    "./daemon-local-rpc-orpc": "./src/daemon-local-rpc-orpc.ts",
    "./socket-message-port": "./src/socket-message-port.ts"
  },
  "scripts": {
    "check": "vp check --no-fmt --no-lint . && vp test run",
    "test": "vp test run",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "@orpc/client": "catalog:",
    "@zendev-lab/spark-protocol": "workspace:^",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@orpc/contract": "catalog:",
    "@orpc/server": "catalog:",
    "@stryker-mutator/core": "catalog:",
    "@stryker-mutator/vitest-runner": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=26.0.0 <27"
  }
}
