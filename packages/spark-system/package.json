{
  "name": "@zendev-lab/spark-system",
  "version": "0.1.0",
  "private": true,
  "description": "Spark filesystem path, permission, command, and local runtime helpers.",
  "files": [
    "src",
    "!src/**/*.test.ts",
    "package.json",
    "README.md"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./strings": "./src/strings.ts",
    "./daemon-local-rpc": "./src/daemon-local-rpc.ts"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "check": "vp check --no-fmt --no-lint . && vp test run",
    "test": "vp test run"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=26.0.0 <27"
  }
}
